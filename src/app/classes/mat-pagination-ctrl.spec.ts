import { MatPaginationCtrl } from './mat-pagination-ctrl';

describe('MatPaginationCtrl', () => {
  it('should create an instance', () => {
    expect(new MatPaginationCtrl()).toBeTruthy();
  });
});
